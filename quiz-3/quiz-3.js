/**
 * INSTRUCTIONS:
 *
 * Write a function that accepts a string sentence as a parameter, and returns a string with the words sorted by length.
 * You can count punctuation symbols as "letters" in the word.
 * (See the examples below for expected output).
 */

function sortWords(sentence) {

}

console.log(sortWords("The quick brown fox jumped over the lazy dog"));
// Output: The fox the dog over lazy quick brown jumped

console.log(sortWords("A wizard's job is to vex chumps quickly in fog"));
// Output: A is to in job vex fog chumps quickly wizard's

console.log(sortWords("Watch 'Jeopardy!', Alex Trebek's fun TV quiz game"));
// Output: TV fun Alex quiz Watch game Trebek's 'Jeopardy!'